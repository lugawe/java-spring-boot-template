# java-spring-boot-template

## Build and run

Build jar with maven:

```bash
./mvnw clean package
```

Run jar with java:

```bash
java -jar target/java-spring-boot-template-1.0.jar
```

## Run tests

Run tests

```bash
./mvnw test
```
